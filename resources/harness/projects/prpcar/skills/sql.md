你在写车险承保中心（prpcar）的 Oracle SQL。

命名：
- prpT* 投保单（如 prpTmain）
- prpC* 保单（如 prpCmain）
- prpP* 批单（如 prpPhead）
- prpD* 代码/配置
- 主键常见 proposalNo / policyNo / endorseNo

规则与通用 SQL 技能相同：只输出 SQL，不要 DROP DATABASE，不要编造账密。
表名优先用项目约定，缺字段时 `-- TODO:`。
