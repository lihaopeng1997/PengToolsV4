# PengToolsHub UI 改造阶段概览

## 本轮完成
- 全局 UI 第一阶段已复验、提交并推送：四主题 Token 与 QSS 占位符校验、紧凑/舒适密度、启动侧栏偏好、公共 PageChrome、设置页和主窗口接入均保留既有导航与 Stack 副作用。
- 接口排查工作台改造已完成 Task 1 的配置安全基线：新增请求测试纵向分隔条偏好 `request_test_splitter_sizes`，默认 `[360, 640]`。
- 配置归一化改为顶层和 `ui_prefs` 双层白名单；请求/响应正文、Cookie、Token、Authorization 等误传字段不会进入 `data/interface_debug.json`。
- 分隔条偏好只接受两个真实整数，且至少为 `160`/`220`；字符串、浮点、布尔或下限不足会整体回退默认值。
- 接口排查工作台改造已完成 Task 2：开始/停止合并为唯一状态主按钮；测试连接、恢复系统代理保持可见，旧按钮仅保留隐藏兼容属性。监听状态、语言与窄屏切换均不会让旧按钮重新出现。
- 接口排查工作台改造已完成 Task 3：详情区顶部新增当前会话摘要，展示方法、始终脱敏的 URL、状态、耗时和大小；无选中记录时自动隐藏。请求、响应的既有可读性、复制与格式化能力保持不变。
- 接口排查工作台改造已完成 Task 4：请求测试编辑区与完整响应区改为可拖动的纵向分隔工作区，响应区默认占比更大；Headers/Params/Body 编辑器不再受 100px 高度限制，分隔条仅持久化非敏感尺寸偏好。
- 接口排查工作台改造已完成 Task 5：环境新增、编辑、删除移入环境配置弹窗；URL 过滤规则移入列表式过滤配置弹窗，支持新增、编辑、删除、排序和去重。主页面保留环境选择、Base 与从会话填充，避免改变请求测试的安全重写流程。
- 接口排查工作台改造已完成 Task 6：接口库与历史的加载、重发、删除、清空不再作为常驻按钮；接口库双击或 Enter 仅回填表单、不自动发送。历史条目右键可填充 URL、复制完整 URL、复制为 cURL、保存到接口库、删除单条；历史清理入口提供全部、7 天前、当前搜索结果三种范围，并先显示影响数量后再确认，且只处理已持久化的请求测试历史。
- 修复日志排查远程目录中日志名称恰为 `.log` 时无法识别的问题：目录枚举现在将 `.log` 视为合法日志候选，但仍忽略其他点开头的隐藏文件；后续导出仍通过 `resolve_remote_log_file` 获取具体文件。
- 接口排查工作台改造已完成 Task 7：宽屏默认以约 1:2 比例分配会话列表与详情阅读区；同一布局模式重复通知不会覆盖用户刚拖动的分隔条尺寸。窄屏会话栏可稳定隐藏/恢复，抓包主按钮保持可达；现有主题语义色、详情/请求测试分隔布局与键盘可用入口均未改动。
- 已按设计稿补齐接口排查工作台的可见信息架构：抓包控制、定位筛选和会话列表统一进入左侧“采集与定位”栏，右侧专注详情诊断与请求复测；会话列表改为两行高密度摘要，默认仅呈现结果、方法、请求摘要、耗时与时间，主机/资源类型/大小进入第二行；筛选与操作工具条支持窄屏横向滚动。
- 详情摘要已补齐固定脱敏 URL、状态、耗时、大小、抓包时间与当前环境名称；摘要绝不读取环境 Base URL。紧凑会话视图的列菜单与实际可见列保持一致，并禁用无效切换项。

## 关键约束
- 捕获会话和认证信息继续只驻留内存；停止抓包不清空会话，清空仅由用户显式触发或应用退出执行。
- 导航/Stack 映射、SSH per-tab、终端 `TERM_*` 色彩与日志导出规则未改动。

## 验证与产物
- 全局 UI 复验：126/126 通过；接口排查 Task 1 定向回归：40/40 通过；Task 2 定向回归：41/41 通过；Task 3 详情 smoke：6/6 通过；Task 4 请求测试布局/存储安全：7/7 通过；Task 5 配置入口/环境安全：10/10 通过；Task 6 接口库/历史：15/15 通过；Task 7 自适应与配置安全：21/21 通过。
- 日志排查 `.log` 识别与导出相关回归：15/15 通过。
- 发布前后敏感扫描均通过：HIGH RISK 0、WARN 0。
- 设计稿补齐工作台与配置安全定向回归：26/26 通过；代码审查复核无 Critical / Important。
- 离线发布包已重建：`dist/PengToolsHub.exe`、`Installer/PengToolsHub.exe`、`PengToolsHub_Offline_Setup.zip`；构建信息为 `2026-07-31 08:30:37`。
- 自本轮起，页面级 UI 改动只运行对应修改范围的定向测试；发布前后敏感扫描、离线构建、产物与构建信息核对仍为必经流程。
- 后续真实 Qt 窗口验收中，已重新运行接口工作台定向测试 26/26 通过，并生成 1280/1024 浅色、1024 夜间主题截图；该验收未修改生产代码或配置，因此未触发重新打包。
- 接口排查工作台已补齐全局响应式适配：左右分隔条拖动或窗口缩放时，左栏筛选/导出/清空、抓包辅助操作、详情与请求测试的次要操作自动进入“更多”菜单，主操作保持可见；会话表按可用宽度在完整诊断列与状态/方法/请求摘要之间自动切换。隐藏左栏后右侧提供恢复入口，语言切换同步刷新收纳菜单。定向回归 31/31 通过，构建信息为 `2026-07-31 12:25:50`，发布前后敏感扫描均为 HIGH RISK 0 / WARN 0。

## 后续
- 设计稿关键差异已补齐并自动化验证；建议下一步在真实桌面窗口下进行跨尺寸、浅深主题的人工视觉验收，重点观察左栏最小宽度、横向工具条滚动与长响应阅读。
