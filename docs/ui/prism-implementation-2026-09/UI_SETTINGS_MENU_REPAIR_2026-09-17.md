# 设置保存与菜单续修

用户运行软件后指出设置、菜单等展示仍与设计有差距，并补充内网模型保存与应用保存冲突、应用保存缺少 loading。此记录是续修范围及证据，不代表整套 UI 已验收。

基线：`D:\PengTools`，`ui/prism-v1`，`ce01d8166ed94f411bbd4d917cff938dc887fbc8`。本轮 fetch 因 GitHub TLS 握手失败，当前远端一致性未重新确认；不提交或推送。前包 Agent 未提交改动及原有 13 个遗留文件继续保留。

实施分工：Luna Max 设置代理负责保存链和页面最小展示修复，菜单代理负责导航展示；主代理负责设计与边界、源码审查、定向验收。设置代理独占 `panels/settings_panel.py`、必要 `_apply_settings` 段和设置测试；菜单代理不修改这些段。

设置验收目标：应用保存不覆盖内网模型草稿；模型单独保存不应用一般偏好；当前提醒编辑不会在总保存前被重载；保存有真实忙碌和完成/失败反馈，禁止重复执行，失败保留编辑。使用内存配置和隔离测试，不操作用户窗口中的真实保存。

菜单验收目标：以最新原型和窗口续修决定为准，导航入口与权限仍由 `navigation_model` 提供；收起轨道菜单可达、选中与键盘关闭正确，弹层不销毁业务会话；不新增原型模拟通知、导出或设计展板业务入口。Vue 源码与生成资源同步，不手改 bundle。

当前：实施中。代理不主动关闭用户窗口；后续源码更新需重新打开才能加载，重启必须保留未保存内容及现有会话。

设置段已冻结并经主代理复核：`tests.test_settings_save_repair` + `tests.test_prism_settings_navigation` 共 9 项通过。测试调用实际 MainWindow 保存方法，覆盖跨保存域草稿、模型点击快照、互斥、失败保留及写入后应用异常回执。设置代理另运行联合定向 66 项通过；不作为真实用户配置保存证据。宽屏与窄屏隔离截图已查看，中文可读，宽屏标签与控件中心偏差不超过 4px，设置类别栏 180px、间距 24px。截图见 `scripts/diagnostics/settings_save_repair_preview/`。测试与预览不读写真实配置或凭据。

用户再次强调 UI 是重点，并询问是否打开了错误分支。主代理核对启动路径为 `D:\PengTools\run.py`、当前分支和 SHA 如上；`verify:embedded` 通过仅证明资源引用及协议完整，不能证明设计一致。源码目前未实现后来批准的主窗口自绘标题栏和全局打开模块标签，侧栏仍为内联展开。属于实施未完成，不能用原先保留系统边框的旧规格解释为已经达标。设置、菜单先按本记录修复，顶部与模块标签继续按已批准续修范围推进；窗口生命周期验收单列。

增加独立 Luna Max 顶部/标签代理：先新增窗口控件与标签组件、模拟测试，禁止与其他代理同时修改主窗。主窗保存段先由设置代理负责，菜单/顶部集成随后交单一代理。置顶仅本窗口临时状态，不新增设置键；标签关闭只移除导航入口，当前标签回邻近保留标签或首页，仍走原导航离页流程。Qt 系统拖动/缩放优先，实际 Snap、多屏及 DPI 不得仅凭模拟测试宣称通过。

组件阶段主代理证据：标题栏/标签 13 项通过；菜单组件 6 项通过。字体预览初版中文方块已退回，修正的 Windows 预览中文可读；这只是组件预览，尚不代表生产主窗已接入。主代理使用与启动相同的 Microsoft YaHei UI + calm 配置做无业务数据字体探针，中文 glyph 均非零，未改产品字体资源。

`tests.test_prism_sidebar_menu` + `tests.test_web_shell` 联合 57 项中 56 项通过，1 项 `EXISTING_FAILURE`：`JsHandshakeOrderTest.test_dashboard_page_ready_after_render` 使用源码中字符串首次索引判断 dashboard 握手，得到 1752 < 2976。所读取的 `frontend/src/dashboard/main.ts` 和 `tests/test_web_shell.py` 与 HEAD 均无差异；本轮不修该无关静态测试。旧运行器黑色主题断言也未作为本轮单主题视觉通过证据。新增真实窄 WebView 弹层测试另行补齐。

真实窄 WebView 证据已补齐：主代理独立运行 `tests.test_prism_native_menu_runtime` 1 项通过，使用 Windows Qt 平台、生产 Chrome 资源、真实 HomeBridge，逻辑宽度 72/248px、DPR 1.5。248px 原生弹层实际越过 WebView 边界，叶子只发一次导航信号，Esc 关闭与焦点恢复通过；无原生能力时 DOM fallback 路径也通过。本地 file 允许、HTTPS 拒绝。报告与截图在 `scripts/diagnostics/prism_native_menu_runtime/`。该测试独立运行，不能在已有 offscreen QApplication 中把跳过算通过；无真实配置、业务 Panel 或网络 I/O。

主代理另独立复核窗口框架、标题栏、模块标签及架构测试共 24 项通过。正式主窗集成进行中，Snap、多屏混合 DPI、真实业务关闭确认与用户视觉接受仍待后续证据。

2026-09-18 恢复：分支和 SHA 未变，工作区保留前轮全部未提交改动。Luna 主窗代理上轮因额度停止，主代理已恢复其收尾任务。主代理重新运行主窗隔离集成、设置、侧栏、标题栏、标签、框架、架构共 39 项通过，真实 Windows 窄 WebView 专项独立 1 项通过。

上轮实际 Windows `show()` 曾触发 access violation；独立隔离证据定位为未处理消息调用 `super().nativeEvent(...)`。改为直接返回 `(False, 0)` 后，生产 MainWindow 布局方法在真实 Windows Qt 宿主可显示、处理事件并正常退出，标题栏、公共账户和作者入口可见。该宿主跳过原构造启动流程、服务和业务 Panel，使用内存偏好，无真实数据库或配置 I/O。主窗按钮、关闭链和截图仍待本次收尾验证，不把这个烟测算作完整窗口行为验收。

2026-09-18 代码复核包冻结：主窗已接入标题栏、File/View/Help 原动作、模块标签、Prism 原生侧栏及 Web 原生菜单桥。作者、账户和折叠入口在公共标题栏，不依赖隐藏 fallback。标签关闭只移除导航项；当前项 fallback 创建失败会恢复当前标识、标签及原 stack，连续失败保护测试通过。最大化菜单文字随窗口状态更新。设置保存段保留上轮修复。

用户新增授权：纯代码先提交 Git 供 Web GPT 检查，截图/窗口效果留本机验收。提交范围仅本轮 UI 源码、生成 Web 资源、测试/隔离诊断脚本及本记录和 `WEB_UI_REVIEW_2026-09-18.md`；原 13 个遗留文件、先前 Agent 包及 PNG/报告 JSON 不提交。主代理最后独立 39 项 + Windows WebEngine 1 项通过，typecheck、verify:embedded 和 diff 检查通过；原 13 文件哈希全部一致。Luna 自测另报告 10 项通过，不将旧业务 fixture 当作本包隔离启动证据。

发布前 fresh fetch 成功，`origin/ui/prism-v1` 仍为基线 `ce01d8166ed94f411bbd4d917cff938dc887fbc8`。这取代上轮 TLS 失败的一致性状态。本地仍保留无关未提交改动，因此 Web 复核必须锁定发布提交，不能假定本地整棵树干净或等同远端。物理窗口按钮、原关闭/托盘/确认链、实际拖动/缩放/Snap、多屏混合 DPI、正式主窗截图及用户视觉验收尚待本机；未宣称整套 UI 完成。
