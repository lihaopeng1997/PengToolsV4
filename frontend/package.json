{
  "name": "pengtools-frontend",
  "private": true,
  "version": "0.0.0",
  "description": "PengToolsHub V4 Web UI 迁移目标工程（Vue 3 + TypeScript + Vite，Qt WebEngine file:// 离线运行）",
  "scripts": {
    "typecheck": "vue-tsc --noEmit",
    "build": "vite build",
    "verify:dist": "node scripts/verify-dist.mjs",
    "check": "npm run typecheck && npm run build && npm run verify:dist"
  },
  "dependencies": {
    "vue": "3.5.42"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "6.0.8",
    "typescript": "6.0.2",
    "vite": "8.2.2",
    "vue-tsc": "3.3.11"
  }
}
